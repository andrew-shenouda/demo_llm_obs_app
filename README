First, run: 
python3 -m venv venv
source venv/bin/activate

Only on first install, run:
pip install fastapi uvicorn openai python-dotenv

Then to run the backend:
uvicorn main:app --reload

To set up the frontend:
Run "open index.html".